# الكتيبات التشغيلية — النواة

## الهدف
كتيبات التشغيل والصيانة. ماذا تفعل في كل حالة. خطوات لكل سيناريو.

## الواجهة
- `README.md`


## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- كتابة كتيبات لكل سيناريو
- ربطها بالتنبيهات
- تحديثها دورياً

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "docs_runbooks: OK" || echo "docs_runbooks: FAIL"
```
