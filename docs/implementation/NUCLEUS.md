# التنفيذ — النواة

## الهدف
أدلة التنفيذ. كيف تُبنى كل وحدة خطوة بخطوة. من المخطط إلى الكود.

## الواجهة
- `90-day-plan.md`
- `README.md`
- `database-schema.sql`
- `event-contracts.md`
- `phase0_gap_report.md`
- `service-map.md`
- `status.md`


## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- كتابة أدلة التنفيذ
- ربطها بالنتائج
- تحديثها عند كل تغيير

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "docs_implementation: OK" || echo "docs_implementation: FAIL"
```
