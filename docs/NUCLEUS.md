# Docs — النواة

## الهدف
التوثيق: خريطة البنية، حالة كل وحدة، دليل الوكيل الجديد، القرارات المعمارية، الكتيبات التشغيلية.

## الواجهة
- `adr/` — قرارات العمارة
- `blueprints/` — المخططات
- `glossary.md` — المسرد
- `implementation/` — التنفيذ
- `runbooks/` — الكتيبات التشغيلية

## الحالة
stub — الهيكل موجود

## الخطوات التالية
- تحديث دليل الوكيل الجديد ليعكس البنية الجديدة
- توثيق حالة كل وحدة بصدق
- إنشاء مخطط البنية الكامل

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "docs: OK" || echo "docs: FAIL"
```
