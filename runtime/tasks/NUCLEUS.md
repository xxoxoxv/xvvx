# إدارة المهام — النواة

## الهدف
إدارة المهام. كل مهمة لها حالة ووكيل وخطة ونتيجة. لا مهمة ضائعة.

## الواجهة
- (يحدد لاحقاً عند البناء)

## قاعدة البيانات
- `tasks`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط المهام بقاعدة البيانات
- تفعيل التتبع
- تعريف الأولويات
- لوحة مهام

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "runtime_tasks: OK" || echo "runtime_tasks: FAIL"
```
