# محرك التنفيذ — النواة

## الهدف
محرك تنفيذ الوكلاء. يستقبل المهام ويوزعها ويراقبها. قلب الدولة النابض.

## الواجهة
- (يحدد لاحقاً عند البناء)

## قاعدة البيانات
- `tasks`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط المحرك بقاعدة البيانات
- تفعيل التنفيذ
- تعريف دورة حياة المهمة

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "runtime_engine: OK" || echo "runtime_engine: FAIL"
```
