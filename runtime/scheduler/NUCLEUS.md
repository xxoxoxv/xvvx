# المجدول — النواة

## الهدف
مجدول المهام. يوزع الموارد ويحدد الأولويات ويتعامل مع الازدحام.

## الواجهة
- (يحدد لاحقاً عند البناء)


## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- تعريف سياسات الجدولة
- تفعيل الأولويات
- إنشاء طوابير متعددة

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "runtime_scheduler: OK" || echo "runtime_scheduler: FAIL"
```
