# إدارة الأحداث — النواة

## الهدف
إدارة الأحداث المركزية. كل حدث موثق وموزع. Event Sourcing كامل.

## الواجهة
- (يحدد لاحقاً عند البناء)

## قاعدة البيانات
- `event_store`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط الأحداث بقاعدة البيانات (157 حدث)
- تفعيل Event Sourcing
- نظام اشتراكات

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "runtime_events: OK" || echo "runtime_events: FAIL"
```
