# Runtime — النواة

## الهدف
محرك التشغيل: تشغيل الوكلاء فعلياً، إدارة المهام، الأحداث، وذاكرة النماذج. النواة النائمة الآن.

## الواجهة
- إدارة المهام (Supabase: tasks)
- سجل الأحداث (Supabase: event_store — 157 حدث)
- ذاكرة النماذج (Supabase: model_cache)
- تشغيل الوكلاء وإدارة دورة حياتهم

## الحالة
stub — جديد، نائم، ينتظر التفعيل

## الخطوات التالية
- تفعيل محرك تنفيذ المهام
- ربط سجل الأحداث بآلية Event Sourcing
- إنشاء واجهة تشغيل الوكلاء

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "runtime: OK" || echo "runtime: FAIL"
```
