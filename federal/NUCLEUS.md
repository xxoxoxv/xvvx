# Federal — النواة

## الهدف
الحكومة المركزية. تنسق بين الولايات والمؤسسات، وتدير الميزانية والسياسات المشتركة.

## الواجهة
- `executive/` — السلطة التنفيذية
- `judicial/` — السلطة القضائية
- `legislative/` — السلطة التشريعية
- `treasury/` — الخزانة الفدرالية

## الحالة
stub — الهيكل موجود والمحتوى ينتقل من المستودعات القديمة

## الخطوات التالية
- ربط الخزانة بقاعدة البيانات (Supabase: treasury_transactions, treasury_budgets, treasury_reports)
- تفعيل آلية التنسيق بين الولايات
- إنشاء سياسات مشتركة للنشر والإصدار

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "federal: OK" || echo "federal: FAIL"
```
