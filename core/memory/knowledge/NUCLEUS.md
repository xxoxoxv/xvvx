# المعرفة — النواة

## الهدف
القاعدة المعرفية للدولة. حقائق ومفاهيم وقواعد يستخدمها الوكلاء.

## الواجهة
- `README.md`
- `vector-store/`


## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- بناء قاعدة معرفية قابلة للبحث
- تصنيف المعرفة بالمجالات
- ربطها بذاكرة الوكلاء

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "core_memory_knowledge: OK" || echo "core_memory_knowledge: FAIL"
```
