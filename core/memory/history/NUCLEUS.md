# التاريخ — النواة

## الهدف
سجل أحداث الدولة. كل قرار وكل تغيير وكل مهمة موثقة هنا للأبد.

## الواجهة
- `README.md`
- `archives/`
- `biographies/`
- `chronology/`

## قاعدة البيانات
- `event_store`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط السجل التاريخي بسجل الأحداث
- إنشاء خط زمني تفاعلي
- تفعيل البحث التاريخي

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "core_memory_history: OK" || echo "core_memory_history: FAIL"
```
