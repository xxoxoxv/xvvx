# الذاكرة التشغيلية — النواة

## الهدف
بيانات الجارية. حالة اللحظة الحالية للدولة ووكلائها ومؤسساتها.

## الواجهة
- `README.md`
- `short-term.md`

## قاعدة البيانات
- `memories`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط الذاكرة التشغيلية بقاعدة البيانات
- تفعيل التحديث الفوري
- إنشاء لقطة حالة

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "core_memory_operational: OK" || echo "core_memory_operational: FAIL"
```
