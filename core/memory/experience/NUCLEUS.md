# الخبرات — النواة

## الهدف
دروس مستخلصة من المهام السابقة. كل خبرة تحسن أداء الوكلاء في المهام المشابهة.

## الواجهة
- `README.md`
- `failure/`
- `gaps/`
- `success/`

## قاعدة البيانات
- `experiences`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- استيراد الخبرات من قاعدة البيانات
- تصنيفها حسب النوع والجودة
- ربطها بالمهام المتكررة

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "core_memory_experience: OK" || echo "core_memory_experience: FAIL"
```
