# الذاكرة — النواة

## الهدف
ذاكرة الدولة الرقمية. الخبرات، التاريخ، المعرفة، والذاكرة التشغيلية.

## الواجهة
- `README.md`
- `experience/`
- `history/`
- `knowledge/`
- `operational/`

## قاعدة البيانات
- `memories`
- `experiences`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط الذاكرة بقاعدة البيانات
- تفعيل البحث المعرفي
- إنشاء آلية استرجاع ذكية

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "core_memory: OK" || echo "core_memory: FAIL"
```
