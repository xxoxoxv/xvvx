# Core — النواة

## الهدف
القلب الدستوري للاتحاد. يحتوي على الدستور والذاكرة والميثاق. يحدد القيم والمبادئ والحدود والقرارات الأساسية التي تحكم كل الكيانات في الدولة الرقمية.

## الواجهة
- `constitution/` — الدستور: الديباجة، المواد، التعديلات، التفسيرات
- `memory/` — الذاكرة: الخبرات، التاريخ، المعرفة، الذاكرة التشغيلية
- `meta/` — الميثاق: Charter، المساهمون، خارطة الطريق، القوالب، الإصدارات

## الحالة
stub — الهيكل منقول والمحتوى موجود

## الخطوات التالية
- توحيد هوية الملفات عبر كل الأقسام (ترويسة تعريفية لكل ملف)
- ربط الذاكرة بقاعدة البيانات (Supabase: memories, experiences)
- تفعيل آلية البحث الدستوري

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "core: OK" || echo "core: FAIL"
```
