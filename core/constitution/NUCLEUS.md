# الدستور — النواة

## الهدف
القانون الأعلى للدولة. يحدد القيم والمبادئ والحدود والقرارات الأساسية التي تحكم كل الكيانات.

## الواجهة
- `README.md`
- `amendments/`
- `articles/`
- `interpretations/`
- `preamble.md`

## قاعدة البيانات
- `king_decrees`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- مراجعة المواد الدستورية
- ربط المراسيم الملكية بقاعدة البيانات
- تفعيل البحث الدستوري

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "core_constitution: OK" || echo "core_constitution: FAIL"
```
