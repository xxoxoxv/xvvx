# اختبارات الدخان — النواة

## الهدف
اختبارات سريعة تتحقق أن كل نواة تعمل. أول خط دفاع. ثوانٍ لا دقائق.

## الواجهة
- (يحدد لاحقاً عند البناء)


## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- إنشاء اختبار لكل مجال
- ربطه بـ CI/CD
- تفعيل التشغيل التلقائي

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "tests_smoke: OK" || echo "tests_smoke: FAIL"
```
