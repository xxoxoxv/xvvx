# اختبارات التكامل — النواة

## الهدف
اختبارات التفاعل بين الوحدات. كل تكامل موثق. لا مفاجآت عند الربط.

## الواجهة
- (يحدد لاحقاً عند البناء)


## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- تعريف سيناريوهات التكامل
- تفعيل الاختبارات الآلية
- توثيق التبعيات

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "tests_integration: OK" || echo "tests_integration: FAIL"
```
