# واجهات API — النواة

## الهدف
واجهات برمجة التطبيقات. نقاط نهاية REST و GraphQL. عقد واضح لكل خدمة.

## الواجهة
- (يحدد لاحقاً عند البناء)


## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- تعريف نقاط النهاية
- توثيق المخططات
- تفعيل المصادقة
- وثائق API تفاعلية

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "interfaces_api: OK" || echo "interfaces_api: FAIL"
```
