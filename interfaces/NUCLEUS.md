# Interfaces — النواة

## الهدف
الواجهات: توليدها حسب احتياج المالك أو المستخدم. كل واجهة معرفة بمسار ونوع وحالة.

## الواجهة
- سجل الواجهات (Supabase: interface_registry — جاهز لكن فارغ)
- كل واجهة لها route_path و html_content و interface_type
- توليد الواجهات يكون عند الطلب من المالك

## الحالة
stub — جديد، ينتظر التأسيس

## الخطوات التالية
- تفعيل آلية توليد الواجهات
- ربط السجل بقاعدة البيانات
- إنشاء قوالب واجهات أساسية

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "interfaces: OK" || echo "interfaces: FAIL"
```
