# الواجهات الويب — النواة

## الهدف
واجهات الويب. صفحات وتطبيقات للمستخدم والمالك. كل واجهة مسجلة.

## الواجهة
- (يحدد لاحقاً عند البناء)

## قاعدة البيانات
- `interface_registry`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط الواجهات بقاعدة البيانات
- تفعيل التوليد عند الطلب
- قوالب موحدة

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "interfaces_web: OK" || echo "interfaces_web: FAIL"
```
