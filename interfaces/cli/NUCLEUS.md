# واجهة سطر الأوامر — النواة

## الهدف
واجهة سطر الأوامر. تحكم نصي كامل بالدولة. من الطرفية إلى كل شيء.

## الواجهة
- (يحدد لاحقاً عند البناء)


## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- تعريف الأوامر
- ربطها بالواجهات البرمجية
- إنشاء مساعدة تلقائية

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "interfaces_cli: OK" || echo "interfaces_cli: FAIL"
```
