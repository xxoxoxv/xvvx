# Ops — النواة

## الهدف
العمليات: النشر، المراقبة، إدارة الأزمات، النسخ الاحتياطي، والاستمرارية. عين الدولة المفتوحة على كل شيء.

## الواجهة
- `observability/` — المراقبة: اللوحات، السجلات، المقاييس، التتبع
- `continuity/` — الاستمرارية: الأرشيف، التعافي من الكوارث، التكرار، الخلافة، كبسولة الزمن

## الحالة
stub — الهيكل منقول والمحتوى موجود

## الخطوات التالية
- تفعيل لوحات المراقبة
- ربط سجل التدقيق (Supabase: audit_entries — 10 سجلات)
- تفعيل آلية النسخ الاحتياطي والتعافي

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "ops: OK" || echo "ops: FAIL"
```
