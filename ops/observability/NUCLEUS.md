# المراقبة — النواة

## الهدف
مراقبة شاملة للدولة. لوحات، سجلات، مقاييس، تتبع. عين الدولة المفتوحة.

## الواجهة
- `README.md`
- `dashboards/`
- `logs/`
- `metrics/`
- `traces/`

## قاعدة البيانات
- `audit_entries`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط المراقبة بقاعدة البيانات
- تفعيل اللوحات
- إنشاء نظام تنبيه

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "ops_observability: OK" || echo "ops_observability: FAIL"
```
