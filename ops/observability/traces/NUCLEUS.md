# التتبع — النواة

## الهدف
تتبع عمليات الوكلاء. كل عملية من البداية للنهاية. شفافية كاملة.

## الواجهة
- `README.md`


## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- تعريف نقاط التتبع
- تفعيل التتبع الموزع
- إنشاء خريطة تدفق العمليات

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "ops_observability_traces: OK" || echo "ops_observability_traces: FAIL"
```
