# الأرشيف — النواة

## الهدف
أرشيف تاريخي لكل قرارات وبيانات وأحداث الدولة. لا شيء يُفقد.

## الواجهة
- `README.md`
- `cold-storage/`
- `formats/`


## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- تعريف سياسات الأرشفة
- تفعيل الأرشفة التلقائية
- إنشاء فهارس قابلة للبحث

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "ops_continuity_archives: OK" || echo "ops_continuity_archives: FAIL"
```
