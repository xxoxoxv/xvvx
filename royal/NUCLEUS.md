# Royal — النواة

## الهدف
واجهة المالك والحوكمة الملكية. يحمي الدستور وينفذ المراسيم ويراقب الالتزام بالسياسات عبر كل الدولة الرقمية.

## الواجهة
- `security/` — الأمن: التشفير، الحواجز، العزل، المفتاح الكهربائي، الفريق الأحمر، سلسلة التوريد
- `governance/` — الحوكمة: الموافقات، التدقيق، السياسات، سجل المخططات

## الحالة
stub — الهيكل منقول والمحتوى موجود

## الخطوات التالية
- تفعيل الحرس الملكي (Supabase: royal_guards)
- ربط المراسيم الملكية (Supabase: king_decrees)
- تفعيل آلية العزل الآمن للوكلاء (Supabase: agent_isolations)

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "royal: OK" || echo "royal: FAIL"
```
