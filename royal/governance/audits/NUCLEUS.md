# التدقيق — النواة

## الهدف
تدقيق مستقل بسلسلة تجزئة. كل عملية موثقة وغير قابلة للتعديل.

## الواجهة
- `README.md`
- `hash-chain/`

## قاعدة البيانات
- `audit_entries`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط التدقيق بقاعدة البيانات
- تفعيل التدقيق الآلي
- إنشاء تقارير التدقيق الدورية

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "royal_governance_audits: OK" || echo "royal_governance_audits: FAIL"
```
