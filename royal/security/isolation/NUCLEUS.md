# العزل — النواة

## الهدف
عزل الوكلاء الذين يشكلون خطراً. كل عزل موثق بسبب وحالة ومدة.

## الواجهة
- `README.md`

## قاعدة البيانات
- `agent_isolations`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط العزل بقاعدة البيانات
- تفعيل آلية الإفراج المشروط
- توثيق إجراءات العزل

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "royal_security_isolation: OK" || echo "royal_security_isolation: FAIL"
```
