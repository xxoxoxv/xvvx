# الحواجز الواقية — النواة

## الهدف
حدود آلية تمنع الوكلاء من تجاوز صلاحياتهم. كل دور له حدود لا يتعداها.

## الواجهة
- `README.md`
- `enforcements/`
- `policies/`


## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- تعريف الحواجز لكل دور
- تفعيل المراقبة الآلية للتجاوزات
- إنشاء نظام تنبيه فوري

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "royal_security_guardrails: OK" || echo "royal_security_guardrails: FAIL"
```
