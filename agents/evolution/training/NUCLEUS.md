# التدريب — النواة

## الهدف
تدريب الوكلاء الجدد. منهج دراسي، امتحانات، تخرج، شهادات.

## الواجهة
- `README.md`
- `data-collectors/`
- `lora-factory/`

## قاعدة البيانات
- `school_results`
- `agent_training_queue`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط التدريب بقاعدة البيانات
- تفعيل طابور التدريب
- إنشاء مناهج لكل تخصص

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "agents_evolution_training: OK" || echo "agents_evolution_training: FAIL"
```
