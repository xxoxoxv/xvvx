# التطور — النواة

## الهدف
تطور الوكلاء عبر التدريب والتقييم والتكرار والتقطير. من ضعيف إلى قوي.

## الواجهة
- `README.md`
- `distillation/`
- `evaluation/`
- `replication/`
- `training/`

## قاعدة البيانات
- `school_results`
- `agent_training_queue`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط التطور بقاعدة البيانات
- تفعيل طابور التدريب
- إنشاء مسارات تطور

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "agents_evolution: OK" || echo "agents_evolution: FAIL"
```
