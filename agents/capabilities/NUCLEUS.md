# القدرات — النواة

## الهدف
مصفوفة مهارات الوكلاء. ما يستطيع كل وكيل فعله، وما لا يستطيع. تصنيف دقيق.

## الواجهة
- `README.md`
- `skill-matrix.md`

## قاعدة البيانات
- `tools`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط القدرات بالأدوات
- تصنيف المهارات بمستويات
- إنشاء اختبارات قدر

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "agents_capabilities: OK" || echo "agents_capabilities: FAIL"
```
