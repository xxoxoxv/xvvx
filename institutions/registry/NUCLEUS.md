# سجل المؤسسات — النواة

## الهدف
السجل الرسمي للمؤسسات. كل مؤسسة موثقة بالنوع والحالة والميزانية.

## الواجهة
- (يحدد لاحقاً عند البناء)

## قاعدة البيانات
- `institutions`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط السجل بقاعدة البيانات (8 مؤسسات)
- تفعيل البحث
- إنشاء لوحة معلومات

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "institutions_registry: OK" || echo "institutions_registry: FAIL"
```
