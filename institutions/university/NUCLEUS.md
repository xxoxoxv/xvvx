# الجامعة الفدرالية — النواة

## الهدف
الجامعة. تخرج الوكلاء الجدد وتمنح الشهارات وتجري البحوث. عقل الدولة.

## الواجهة
- (يحدد لاحقاً عند البناء)

## قاعدة البيانات
- `school_results`
- `agent_training_queue`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط الجامعة بالتدريب
- تفعيل طابور التدريب
- توثيق المناهج
- نظام تخرج

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "institutions_university: OK" || echo "institutions_university: FAIL"
```
