# مصنع الأدوات — النواة

## الهدف
مصنع الأدوات. يولد الأدوات الجديدة للوكلاء عند الطلب. مصنع الدولة.

## الواجهة
- (يحدد لاحقاً عند البناء)

## قاعدة البيانات
- `tool_generation_queue`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط المصنع بقاعدة البيانات
- تفعيل طابور التوليد
- توثيق التصنيع

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "institutions_factory: OK" || echo "institutions_factory: FAIL"
```
