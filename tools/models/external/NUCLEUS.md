# النماذج الخارجية — النواة

## الهدف
نماذج من مزودين خارجيين. كل نموذج موثق المصدر والترخيص والحدود.

## الواجهة
- `README.md`
- `contracts/`
- `providers.md`


## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- جرد النماذج الخارجية
- فحص التراخيص
- مراقبة الأداء والتكلفة

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "tools_models_external: OK" || echo "tools_models_external: FAIL"
```
