# سجل النماذج — النواة

## الهدف
السجل المركزي لكل النماذج. مصدر الحقيقة الوحيد.

## الواجهة
- `README.md`

## قاعدة البيانات
- `model_cache`
- `model_cost_log`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط السجل بقاعدة البيانات
- تفعيل البحث
- إنشاء لوحة معلومات النماذج

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "tools_models_registry: OK" || echo "tools_models_registry: FAIL"
```
