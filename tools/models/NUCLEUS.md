# النماذج — النواة

## الهدف
سجل نماذج الذكاء الاصطناعي. كل نموذج موثق بالقدرات والتكلفة والجودة.

## الواجهة
- `README.md`
- `alpha/`
- `beta/`
- `external/`
- `gamma/`
- `registry/`

## قاعدة البيانات
- `model_cache`
- `model_cost_log`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط النماذج بقاعدة البيانات
- تفعيل ذاكرة التخزين
- مراقبة التكلفة

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "tools_models: OK" || echo "tools_models: FAIL"
```
