# المخططات — النواة

## الهدف
مخططات بيانات الأدوات. المدخلات، المخرجات، الأنواع، القيود.

## الواجهة
- `README.md`
- `sql_query.schema.json`


## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- توثيق مخططات الأدوات
- تفعيل فحص الصحة
- إنشاء وثائق API لكل أداة

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "tools_schemas: OK" || echo "tools_schemas: FAIL"
```
