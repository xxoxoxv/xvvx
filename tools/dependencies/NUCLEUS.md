# التبعيات — النواة

## الهدف
إدارة التبعيات الخارجية. كل أداة لها تبعيات موثقة مع إصدارات وحالة.

## الواجهة
- `README.md`


## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- جرد التبعيات
- فحص التوافق
- مراقبة التحديثات والثغرات

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "tools_dependencies: OK" || echo "tools_dependencies: FAIL"
```
