# حوكمة الأدوات — النواة

## الهدف
سياسات استخدام الأدوات. من يستخدم ماذا، ومتى، ولماذا. صلاحيات وقيود.

## الواجهة
- `README.md`
- `check_repository_identity.py`

## قاعدة البيانات
- `tools`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط الحوكمة بقاعدة البيانات (10 أدوات)
- تعريف سياسات الاستخدام
- تفعيل المراقبة

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "tools_governance: OK" || echo "tools_governance: FAIL"
```
