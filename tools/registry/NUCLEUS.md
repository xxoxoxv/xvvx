# السجل — النواة

## الهدف
السجل الرسمي للأدوات المتاحة للوكلاء. كل أداة موثقة بالوصف والفئة.

## الواجهة
- `README.md`
- `manifests/`
- `tool-index.yaml`

## قاعدة البيانات
- `tools`

## الحالة
stub — النواة جاهزة، المحتوى ينتظر البناء

## الخطوات التالية
- ربط السجل بقاعدة البيانات (10 أدوات)
- تفعيل البحث
- إنشاء فهرس الأدوات

## اختبار الدخان
```bash
test -f NUCLEUS.md && echo "tools_registry: OK" || echo "tools_registry: FAIL"
```
